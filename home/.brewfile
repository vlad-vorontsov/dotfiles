# ~/.brewfile
#
# Use this file to install software via Homebrew when setting up a new Mac.


# Check for potential problems.
doctor

# Update Homebrew itself.
update

# Install Bash 4
install bash

# Install a more up to date version of rsync.
install homebrew/dupes/rsync

# Ruby version management.
install --HEAD rbenv
install ruby-build --without-autoconf --without-pkg-config

# Install some useful software.
install ack
install jhead
install jpeg --universal
install ssh-copy-id
install tree
install unrar
install wget
